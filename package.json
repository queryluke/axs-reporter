{
  "name": "axs-reporter",
  "version": "0.1.0",
  "description": "Runs lighthouse over a number of urls and generates CSV report",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Luke Aeschleman",
  "license": "ISC",
  "devDependencies": {
    "csv": "^4.0.0",
    "lighthouse": "^3.2.1"
  }
}
